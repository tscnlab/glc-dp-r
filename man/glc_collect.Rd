% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/collect.R
\name{glc_collect}
\alias{glc_collect}
\title{Collect compatible file groups}
\usage{
glc_collect(x, standardize = c("lightlogr", "none"))
}
\arguments{
\item{x}{A collection returned by \code{\link[=glc_read]{glc_read()}}.}

\item{standardize}{Either \code{"lightlogr"} to add the conventional \code{Id},
\code{file_group_id}, \code{participant_Id}, \code{Datetime}, and \code{file.name} columns and
remove internal \verb{.glc_*} provenance columns, or \code{"none"} to retain source
and provenance columns unchanged.}
}
\value{
A combined tibble. In LightLogR-standardized output, \code{Id} contains
the dataset id, \code{file_group_id} identifies the source file group,
\code{participant_Id} contains the participant id, an existing source
\code{file.name} column is retained, and the result is grouped by \code{Id}.
}
\description{
Explicitly combines file-group tibbles after checking their columns, types,
factor-level contracts, time zones, modalities, roles, data states, and
relationship consistency. Multiple non-missing device links within one
dataset are rejected.
}
\examples{
\dontshow{if (interactive()) withAutoprint(\{ # examplesIf}
iztech <- glc_open("tscnlab/melidos-iztech-glc-dataset")
collection <- glc_read(
  iztech,
  dataset_id = "MELIDOS_IZTECH_S001",
  file_group = "MELIDOS_IZTECH_S001:17",
  n_max = 10
)
glc_collect(collection)
\dontshow{\}) # examplesIf}
}
