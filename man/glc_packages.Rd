% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/registry.R
\name{glc_packages}
\alias{glc_packages}
\title{List registered Global Light Commons data packages}
\usage{
glc_packages(registry = glc_default_registry(), refresh = FALSE)
}
\arguments{
\item{registry}{Registry JSON URL or path. Defaults to the official registry
or the value of option \code{glcdp.registry_url}.}

\item{refresh}{Whether to bypass the in-session registry cache.}
}
\value{
A \code{glc_registry} tibble with one row per registered repository.
}
\description{
Downloads and flattens the Global Light Commons registry. Both passing and
non-passing current revisions are retained.
}
\examples{
\dontshow{if (interactive()) (if (getRversion() >= "3.4") withAutoprint else force)(\{ # examplesIf}
packages <- glc_packages()
glc_search_packages("guidolin", packages)
\dontshow{\}) # examplesIf}
}
