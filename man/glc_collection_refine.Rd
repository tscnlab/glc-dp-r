% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/collection-refine.R
\name{glc_collection_refine}
\alias{glc_collection_refine}
\title{Refine a collection plan from stable file-group identifiers}
\usage{
glc_collection_refine(plan, file_group, compatibility_id = NULL)
}
\arguments{
\item{plan}{A validated \code{glc_collection_plan} object with supported plan and
refinement-input schema versions. Keep this parent plan after refinement;
the lightweight result refers to it by schema, version, and fingerprint and
does not copy its normalized metadata tables.}

\item{file_group}{A character vector of unique, non-missing stable
\code{file_group_id} values. Every value must be an included member of \code{plan}
and all values must belong to one structural compatibility set. Numeric
declaration indices are not accepted.}

\item{compatibility_id}{Optional single structural compatibility identifier.
When \code{NULL}, the identifier is inferred only if all selected groups share
exactly one set. When supplied, it must equal that set's identifier.}
}
\value{
A lightweight \code{glc_collection_refinement} object described in
\strong{Return structure}.
}
\description{
Recompute final collection units for an in-memory subset of one structural
compatibility set. Refinement validates and reuses the compact facts stored
in a \code{\link[=glc_collection_plan]{glc_collection_plan()}} result. It does not reopen the package, load
metadata, access a network, or inspect measurement contents.
}
\details{
\code{glc_collection_refine()} is the final, fast step after interactive metadata
narrowing. Build \code{\link[=glc_collection_plan]{glc_collection_plan()}} once, choose one row of
\code{plan$compatibility_sets}, and filter its member groups through the typed
tables in \code{plan$groups} and \code{plan$metadata}. Pass only the resulting stable
file-group ids to this function. Input order does not affect the result.

Refinement reapplies the stored structural and relationship rules, recomputes
the active deterministic factor union, and creates request-sensitive final
unit ids. Its \code{units} table is identical to
a fresh \code{\link[=glc_collection_plan]{glc_collection_plan()}} call with the parent's original term,
variable, dataset, and standardization request and with \code{file_group} set to
the refined ids. The restriction-stable \code{compatibility_id} is retained. No
full variable or metadata tables are rebuilt.

Device identity remains linked by stable file-group id, so different devices
in one dataset do not split otherwise compatible groups. A safe active factor
union is reported through the unit's \code{harmonization_required} and
\code{harmonized_variables} fields. \code{glc_read()} and \code{glc_collect()} still validate
actual source values and apply the same union before binding.
}
\section{Validation and conditions}{

Refinement supports plan schema \code{"glc-collection-plan"} version \code{"1.2.0"}
and refinement-input schema \code{"glc-collection-refinement-input"} version
\code{"1.1.0"}. It verifies the compact input fingerprint and checks it against
the parent plan's provenance, request, and group membership. The fingerprint
covers the structural-family contract, each member's declared factor contract,
and relationship facts needed for refinement. It does not cover the larger
normalized metadata snapshot, so validation does not rehash the complete plan.
Earlier plan versions do not contain these facts and must be planned again.

All refinement errors inherit from \code{glcdp_collection_refine_error}.
More specific subclasses are:
\itemize{
\item \code{glcdp_collection_refine_plan} for a value that is not a collection plan;
\item \code{glcdp_collection_refine_version} for an unsupported schema or version;
\item \code{glcdp_collection_refine_incomplete} and
\code{glcdp_collection_refine_tampered} for missing or changed parent facts;
\item \code{glcdp_collection_refine_file_group},
\code{glcdp_collection_refine_empty}, and \code{glcdp_collection_refine_duplicate}
for invalid file-group selectors;
\item \code{glcdp_collection_refine_unknown_group} and
\code{glcdp_collection_refine_excluded_group} for groups outside the eligible
parent membership;
\item \code{glcdp_collection_refine_compatibility},
\code{glcdp_collection_refine_unknown_compatibility}, and
\code{glcdp_collection_refine_compatibility_mismatch} for invalid structural
identifiers;
\item \code{glcdp_collection_refine_cross_structure} when selected groups span more
than one structural set; and
\item \code{glcdp_collection_refine_unresolved_identity} when required study,
participant, or device identities cannot be resolved from the stored
metadata facts.
}
}

\section{Zero-access assurance}{

Refinement performs no file or network input/output. It does not call
\code{\link[=glc_open]{glc_open()}}, \code{\link[=glc_files]{glc_files()}}, \code{\link[=glc_summary]{glc_summary()}}, \code{\link[=glc_read]{glc_read()}}, \code{\link[=glc_collect]{glc_collect()}},
\code{\link[=glc_download]{glc_download()}}, or metadata-loading and materialization functions. Its
assurance records that the package was not reopened, remote availability was
not probed, and measurement contents were neither transferred nor inspected.
\code{\link[=glc_read]{glc_read()}} and \code{\link[=glc_collect]{glc_collect()}} remain authoritative after refinement.
}

\section{Return structure}{

The result is a plain serializable list with class
\code{glc_collection_refinement}, schema \code{"glc-collection-refinement"}, and
version \code{"1.1.0"}. It contains:
\itemize{
\item \code{parent}: \code{plan_schema}, \code{plan_version}, and the validated refinement-input
\code{fingerprint} linking this result to the retained parent plan;
\item \code{provenance}: \code{package_id}, \code{repository}, exact \code{source_revision}, and
\code{package_schema_version};
\item \code{request}: the selected \code{compatibility_id}, sorted \code{file_group} ids, and
the parent's compact \code{original} request;
\item \code{assurance}: declaration basis plus package, network, availability-probe,
measurement-transfer, measurement-inspection, and final-validation fields;
\item \code{compatibility_id}, \code{final_selection_required}, and \code{preferred_unit_id};
\item \code{units}: the same stable final-unit columns documented for
\code{\link[=glc_collection_plan]{glc_collection_plan()}}, including active factor harmonization fields;
\item \code{groups}: \code{status}, \code{unit_id}, \code{compatibility_id}, \code{dataset_id}, integer
\code{file_group}, stable \code{file_group_id}, \code{study_id}, \code{participant_id},
\code{participant_associated}, \code{study_link_status}, \code{participant_link_status},
\code{device_id}, \code{device_link_status}, and list-columns \code{reason_codes} and
\code{messages}; and
\item \code{constraints}: \code{compatibility_id}, \code{code}, \code{message}, and logical
\code{resolved}. Its schema is stable even when it has no rows.
}

All tables and list-columns contain only plain serializable values. The
result contains no package handle, environment, token, cache path, or
temporary path. \code{print()} reports the structural id, final-unit and group
counts, final-selection status, and zero-access assurance, then returns the
result invisibly.
}

\examples{
\dontrun{
# Use an existing local, manifest-backed package directory.
# This pattern performs no network request and does not read measurements.
pkg <- glc_open("path/to/manifest-backed-package", quiet = TRUE)
plan <- glc_collection_plan(
  pkg,
  terms = "photopic illuminance",
  variable_scope = "matched"
)

# In an application, filter these ids with plan$groups and plan$metadata.
set <- plan$compatibility_sets[1L, ]
selected_ids <- set$file_group_ids[[1L]]
refined <- glc_collection_refine(
  plan,
  selected_ids,
  compatibility_id = set$compatibility_id[[1L]]
)
refined
}
}
\seealso{
\code{\link[=glc_collection_plan]{glc_collection_plan()}} for the required parent plan,
\code{\link[=glc_read]{glc_read()}} and \code{\link[=glc_collect]{glc_collect()}} for runtime validation and collection.
}
