% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/package.R
\name{glc_open}
\alias{glc_open}
\title{Open a Global Light Commons data package}
\usage{
glc_open(
  source,
  ref = "latest_pass",
  token = NULL,
  cache_dir = NULL,
  registry = NULL,
  quiet = FALSE
)
}
\arguments{
\item{source}{Registry id, \code{owner/repository}, GitHub URL, local package
directory, or local \code{datapackage.json} path.}

\item{ref}{Remote revision: \code{"latest_pass"}, \code{"current"}, or an exact
40-character commit SHA.}

\item{token}{Optional GitHub token. When omitted, \code{GITHUB_PAT} and then
\code{GITHUB_TOKEN} are consulted.}

\item{cache_dir}{Optional explicit persistent cache directory. The default
uses session-temporary storage for remote reads.}

\item{registry}{Optional registry object, URL, or local JSON path.}

\item{quiet}{Suppress informational messages. Warnings remain visible.}
}
\value{
A \code{glc_package} handle.
}
\description{
Opens a local package or resolves a GitHub-hosted package at an immutable
commit. Registered packages default to their latest passing revision.
}
\examples{
\dontshow{if (interactive()) (if (getRversion() >= "3.4") withAutoprint else force)(\{ # examplesIf}
package <- glc_open("tscnlab/guidolin-glee-datasetv2")
package
\dontshow{\}) # examplesIf}
}
